int STARTUP_TIME = maybe 30 seconds min, 60 seconds max

class Generic Crawler{

    ///use this list as a queue to make off where we have been and not been
    list_of_iterables = [filled list of iterables] //needs an ftp connection in order to iterate, therefore crawler

    while( list_of_iterables.length > 0){
        non-critical stuff...
        try{
            critical section where one accesses ftp
            do the ftp buisiness
        }//endtry
        catch(all of the errors){
            wait for some time so that we dont piss off the server
            create a new host and continue with the data until all the iterables are iterated through
        }//endcatch

    }//endwhile

}//end function